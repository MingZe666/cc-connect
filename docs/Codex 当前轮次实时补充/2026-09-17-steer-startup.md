# 补充启动竞态修复

用户已批准：启动中暂存补充，确认当前轮后有序提交；停止或超时明确未提交，未知投递不重试。

1. 新增延迟 Send 的回归测试，验证补充不会提前调用。
2. core 增加每次启动的独立完成信号，沿用消息副本、接收记录、单 worker、完成屏障。
3. Agent 可选能力声明用于进程未创建时登记；Codex app_server 的 Send 返回表示轮次启动确认。
4. 占位 state 替换保留启动批次及补充队列；停止和失败唤醒等待。
5. 验证连续轮次、占位迁移、取消失败和普通队列兼容；运行定向测试、CUJ、build/vet，记录环境限制。

不自动替换运行程序或重启生产实例。
